/**
 * Tab entry for the tenant's rental space. The screen itself lives in
 * app/rental/index so the detail routes (leases, applications) sit beside it
 * outside the tab bar.
 */
export { default } from "../rental/index";
